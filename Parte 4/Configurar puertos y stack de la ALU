.include "m328pdef.inc"

.org 0x0000
    rjmp RESET

RESET:

    ldi r16, high(RAMEND)
    out SPH, r16

    ldi r16, low(RAMEND)
    out SPL, r16

    ldi r16, 0x00
    out DDRD, r16

    ldi r16, 0b00111000
    out DDRB, r16

    ldi r16, 0b00001111
    out DDRC, r16

MAIN:
    rjmp MAIN
